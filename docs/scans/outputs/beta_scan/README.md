# Beta Scan Outputs

This directory is populated by running `run_stride_beta_scan.py`.

Expected outputs:
- `stride_output_beta_<value>.nc` - STRIDE NetCDF output for each beta value
- `beta_scan_summary.csv` - Aggregated delta-prime and delta-W values
