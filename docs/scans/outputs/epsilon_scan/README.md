# Epsilon Scan Outputs

This directory is populated by running `run_stride_epsilon_scan.py`.

Expected outputs:
- `stride_output_eps_<value>.nc` - STRIDE NetCDF output for each epsilon value
- `epsilon_scan_summary.csv` - Aggregated delta-prime and delta-W values
